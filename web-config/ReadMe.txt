# What is this
This is the source of web config. 

# How to use
There is no fancy framework, just javascripts and html so just using any HTTP web server to host it such as Nginx. 
The root html is index.html, so the access address would be like: https://yourdomain.com/index.html
